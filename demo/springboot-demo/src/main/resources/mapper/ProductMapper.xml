<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootdemo.mapper.ProductMapper">

    <insert id="insert" parameterType="com.example.springbootdemo.entity.Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (name, price, stock)
        VALUES (#{name}, #{price}, #{stock})
    </insert>

    <select id="selectById" resultType="com.example.springbootdemo.entity.Product">
        SELECT id, name, price, stock, create_time, update_time
        FROM product
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="com.example.springbootdemo.entity.Product">
        SELECT id, name, price, stock, create_time, update_time
        FROM product
        ORDER BY id DESC
        LIMIT 1000
    </select>

    <delete id="deleteById">
        DELETE FROM product WHERE id = #{id}
    </delete>

    <update id="updateStock">
        UPDATE product
        SET stock = stock - #{quantity}
        WHERE id = #{id} AND stock >= #{quantity}
    </update>

    <select id="selectByName" resultType="com.example.springbootdemo.entity.Product">
        SELECT id, name, price, stock, create_time, update_time
        FROM product
        WHERE name = #{name}
    </select>

</mapper>
